services:
  laverie:
    build: .
    container_name: club55-laverie
    ports:
      - "4242:5000"
    volumes:
      - ./data:/data
    environment:
      DB_PATH: /data/laundry.db
      TZ: Europe/Paris
      SECRET_KEY: ${SECRET_KEY:-change-me-in-production}
    restart: unless-stopped
